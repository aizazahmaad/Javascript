// word = 'This code generates a random decimal number between 0 (inclusive) and 1 (exclusive), then multiplies it by 10 to get a random number between 0 and 10 (exclusive), and finally, uses Math.ceil() to round the number up to the nearest integer. The result will be an integer between 1 and 10, inclusive.'
// console.log(word.toUpperCase())

//Exercise:
var clothes = []
clothes.push('shirt', 'pants', 'sweater', 'tea shirt', 'waist')
clothes.pop()
clothes.push('socks')
console.log(clothes)
console.log(clothes[2])

var favCar = {}
favCar.color = 'red'
favCar.convertible = true
console.log(favCar)
